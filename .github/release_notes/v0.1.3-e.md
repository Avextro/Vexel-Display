# Vexel Display Emulator v0.1.3-e

# Fixed

- Fixed release workflow failing on cmake config
- Fixed relase workflow failing on build release
- Fixed release workflow failing on package release
- Fixed release workflow not having correct permissions
